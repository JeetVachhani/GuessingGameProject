
public interface TreeInterface 
{
	// returns data which is currently present in the root of the tree. 
	// if nothing it there a null is returned
	public String getDataInRoot();
		
	// returns height of the current tree.
	// if tree is empty will return 0 and throw an warning.
//	public int getHeight();

	// returns the number of nodes currently present in the tree
	// if tree is empty will return 0 and throw an warning.
//	public int numberNodes();
	
}
